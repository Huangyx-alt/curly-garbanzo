接入流程

1.在配置表 reddot添加模块的事件以及处理lua脚本

    event_name  需要注册的事件
    execute  处理的lua脚本
2.编写 execute里面的lua脚本，放在当前文档同级目录下,在

3.在Model数据发生变化时发出事件，一般是服务器通知客户端的回调方法里面调用
    RedDotManager:Check(eventName)

4.在UI初始化时调用RedDotManager:RegisterNode，注册红点对象 

 RedDotManager:RegisterNode(RedDotEvent.mail_reddot_event,"function_Mail",self.red_dot)


