---
--- Generated by EmmyLua(https://github.com/EmmyLua)
--- Created by zlc
--- DateTime: 2020/8/13 14:17
---
local Command = {};
---从预制体中生成一个UI出来
function Command.Execute(notifyName, view, is_click, callback)
    log.y("ShowDialog :", view.viewName)
    if not view then
        return
    end
    -- SceneViewManager.RemoveBindView(view
    if view.viewName == "BattleLoadingView" then
        view.OnAnimEnd()
    else
        view:HideDialog(is_click);
    end
    local callback = callback
    if (callback and type(callback) == "function") then
        callback()
    end
end

return Command;
